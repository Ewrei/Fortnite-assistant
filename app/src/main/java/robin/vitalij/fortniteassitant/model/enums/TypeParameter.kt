package robin.vitalij.fortniteassitant.model.enums

enum class TypeParameter {
    SCORE, AVG_SCORE, SCORE_PER_MIN, SCORE_PER_MATCH, WINS,
    TOP_3, TOP_5, TOP_6, TOP_10, TOP_12, TOP_25,
    KILLS, KILLS_PER_MIN, KILLS_PER_MATCH, DEATHS,
    KD, MATCHES, WIN_RATE, MINUTES_PLAYED, PLAYERS_OUT_LIVED
}